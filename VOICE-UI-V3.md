# Voice UI V3

Không cần chạy SQL lại.

Thay đổi:
- Giao diện phòng voice mới, sạch và gọn hơn.
- Grid thành viên 2 cột trên PC / 1 cột trên mobile.
- Avatar chữ cái, trạng thái mic, hiệu ứng người đang nói.
- Thanh điều khiển mic / đóng phòng / rời phòng mới.
- Hiển thị số người trong phòng.
- Bổ sung phát remote audio (`TrackSubscribed`) + `room.startAudio()`.
- Tự dọn audio element khi rời phòng.

Chỉ cần upload source lên GitHub và chờ Vercel deploy.
